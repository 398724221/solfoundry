170px; height:170px; right:7%; top:-55px; border-radius:50%;
      background:radial-gradient(circle at 35% 35%, #fff8c7, var(--sun)); box-shadow:0 0 70px rgba(255,214,107,.34);
      animation:breathe 6s ease-in-out infinite alternate;
    }
    .hero::after {
      content:""; position:absolute; inset:auto 0 0; height:42%; opacity:.45;
      background:
        radial-gradient(ellipse at 15% 100%, rgba(120,213,181,.42) 0 17%, transparent 18%),
        radial-gradient(ellipse at 28% 105%, rgba(105,189,244,.28) 0 20%, transparent 21%),
        radial-gradient(ellipse at 78% 110%, rgba(120,213,181,.30) 0 21%, transparent 22%);
      pointer-events:none;
    }
    @keyframes breathe { to { transform:translateY(8px) scale(1.03); } }
    .hero-copy, .hero-focus { position:relative; z-index:2; }
    .eyebrow { color:#3b92c8; font-size:12px; font-weight:900; letter-spacing:.14em; }
    .hero h1 { margin:13px 0 12px; font-size:clamp(35px,5vw,68px); line-height:1.02; letter-spacing:-.045em; }
    .hero h1 em { font-style:normal; color:#3b92c8; }
    .hero p { max-width:740px; color:var(--muted); font-size:clamp(15px,1.7vw,18px); margin:0; }
    .hero-meta { display:flex; flex-wrap:wrap; gap:8px; margin-top:22px; }
    .chip { display:inline-flex; align-items:center; gap:7px; padding:8px 11px; background:rgba(255,255,255,.65); border:1px solid var(--line); border-radius:999px; font-size:12px; color:var(--muted); }
    .dot { width:8px; height:8px; border-radius:50%; background:var(--mint); box-shadow:0 0 0 4px rgba(120,213,181,.15); }

    .hero-focus { border:1px solid rgba(255,255,255,.82); background:rgba(255,255,255,.62); border-radius:24px; padding:18px; box-shadow:var(--shadow-soft); }
    .hero-focus-head { display:flex; align-items:center; justify-content:space-between; gap:10px; margin-bottom:12px; }
    .hero-focus-head strong { font-size:15px; }
    .focus-mini { display:grid; gap:8px; }
    .focus-mini-item { display:grid; grid-template-columns:26px 1fr auto; align-items:center; gap:9px; padding:9px 10px; border-radius:13px; background:rgba(255,255,255,.72); border:1px solid rgba(47,91,116,.08); }
    .focus-num { width:25px; height:25px; display:grid; place-items:center; border-radius:9px; font-size:11px; font-weight:900; background:var(--sky-soft); color:#297cae; }
    .focus-mini-item span { font-size:12px; min-width:0; white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
    .focus-mini-item small { color:var(--muted); }

    .section-head { display:flex; justify-content:space-between; align-items:flex-end; gap:16px; margin:32px 3px 13px; }
    .section-head h2 { margin:0; font-size:25px; letter-spacing:-.025em; }
    .section-head p { margin:5px 0 0; color:var(--muted); font-size:13px; }
    .kicker { color:#3b92c8; font-weight:900; letter-spacing:.13em; font-size:11px; }

    .card { background:var(--paper); border:1px solid rgba(255,255,255,.85); box-shadow:var(--shadow-soft); backdrop-filter:blur(14px); border-radius:var(--r-lg); }
    .card.pad { padding:19px; }
    .grid { display:grid; gap:14px; }
    .grid.two { grid-template-columns:repeat(2,minmax(0,1fr)); }
    .grid.three { grid-template-columns:repeat(3,minmax(0,1fr)); }
    .grid.main-side { grid-template-columns:minmax(0,1.45fr) minmax(320px,.55fr); }

    .view-switch { display:flex; gap:5px; padding:4px; border-radius:13px; background:rgba(255,255,255,.62); border:1px solid var(--line); }
    .view-switch button { border:0; padding:7px 11px; border-radius:9px; background:transparent; color:var(--muted); cursor:pointer; }
    .view-switch button.active { background:white; color:var(--ink); box-shadow:0 4px 12px rgba(72,116,139,.08); font-weight:750; }

    .quick-add { display:grid; grid-template-columns:1.1fr .9fr .75fr auto; gap:8px; margin-bottom:14px; }
    .input, .textarea, .select {
      width:100%; border:1px solid var(--line); border-radius:13px; padding:11px 12px; color:var(--ink); background:rgba(255,255,255,.78);
    }
    .textarea { min-height:110px; resize:vertical; }
    .input::placeholder, .textarea::placeholder { color:#9badb8; }

    .priority-board { display:grid; grid-template-columns:repeat(4,minmax(220px,1fr)); gap:11px; align-items:start; overflow:auto; padding-bottom:3px; }
    .quadrant { min-height:390px; border:1px dashed var(--line-strong); border-radius:19px; padding:11px; transition:.18s ease; }
    .quadrant[data-zone="now"] { background:linear-gradient(180deg,var(--coral-soft),rgba(255,255,255,.50)); }
    .quadrant[data-zone="plan"] { background:linear-gradient(180deg,var(--sky-soft),rgba(255,255,255,.50)); }
    .quadrant[data-zone="wait"] { background:linear-gradient(180deg,var(--mint-soft),rgba(255,255,255,.50)); }
    .quadrant[data-zone="park"] { background:linear-gradient(180deg,var(--lavender-soft),rgba(255,255,255,.50)); }
    .quadrant.drag-over { border-color:#3b92c8; box-shadow:inset 0 0 0 2px rgba(105,189,244,.18); transform:translateY(-2px); }
    .quadrant-head { display:flex; align-items:flex-start; justify-content:space-between; gap:8px; margin:2px 2px 10px; }
    .quadrant-head strong { display:block; font-size:14px; }
    .quadrant-head span { color:var(--muted); font-size:11px; }
    .count-pill { min-width:25px; height:25px; display:grid; place-items:center; border-radius:9px; background:rgba(255,255,255,.70); font-size:11px; font-weight:900; }

    .task-card { padding:12px; background:rgba(255,255,255,.88); border:1px solid rgba(47,91,116,.10); border-radius:15px; box-shadow:0 7px 18px rgba(72,116,139,.07); margin-bottom:9px; cursor:grab; }
    .task-card:active { cursor:grabbing; }
    .task-card.selected { box-shadow:0 0 0 2px rgba(105,189,244,.35), 0 9px 25px rgba(72,116,139,.10); }
    .task-top { display:grid; grid-template-columns:auto 1fr auto; gap:8px; align-items:start; }
    .select-task { width:21px; height:21px; border:1px solid var(--line-strong); border-radius:7px; display:grid; place-items:center; cursor:pointer; background:white; font-size:12px; color:white; }
    .task-card.selected .select-task { background:#4aaee8; border-color:#4aaee8; }
    .project-tag { display:inline-flex; max-width:100%; padding:3px 7px; border-radius:999px; background:var(--sun-soft); color:#856716; font-size:10px; font-weight:800; margin-bottom:5px; }
    .task-title { font-weight:820; font-size:13px; line-height:1.35; }
    .task-menu { border:0; background:transparent; color:var(--faint); cursor:pointer; font-size:17px; }
    .task-standard { 