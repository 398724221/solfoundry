<!doctype html>
<html lang="zh-CN" data-scene="morning">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
  <meta name="theme-color" content="#fffaf0" />
  <title>Rainy Living World · 工作驾驶舱</title>
  <style>
    :root {
      --cream: #fffaf0;
      --cream-2: #fff4df;
      --paper: rgba(255,255,255,.84);
      --paper-strong: #fffdf8;
      --ink: #18324a;
      --muted: #657b8e;
      --faint: #98a9b5;
      --sky: #69bdf4;
      --sky-soft: #dff3ff;
      --mint: #78d5b5;
      --mint-soft: #e6f8f1;
      --peach: #ffb58d;
      --peach-soft: #fff0e7;
      --sun: #ffd66b;
      --sun-soft: #fff6cf;
      --coral: #ee7b78;
      --coral-soft: #ffebe8;
      --lavender: #b8a8ed;
      --lavender-soft: #f0ecff;
      --line: rgba(47, 91, 116, .12);
      --line-strong: rgba(47, 91, 116, .22);
      --shadow: 0 18px 55px rgba(72, 116, 139, .13);
      --shadow-soft: 0 8px 28px rgba(72, 116, 139, .09);
      --r-xl: 30px;
      --r-lg: 22px;
      --r-md: 15px;
    }

    * { box-sizing: border-box; }
    html { scroll-behavior: smooth; }
    body {
      margin: 0;
      min-height: 100vh;
      color: var(--ink);
      font-family: Inter, ui-sans-serif, -apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC", "Hiragino Sans GB", "Microsoft YaHei", sans-serif;
      line-height: 1.55;
      background:
        radial-gradient(circle at 14% 10%, rgba(255, 214, 107, .30), transparent 24%),
        radial-gradient(circle at 88% 6%, rgba(105, 189, 244, .22), transparent 28%),
        linear-gradient(180deg, #edf9ff 0, var(--cream) 29%, #fffdf8 100%);
      overflow-x: hidden;
    }

    body::before, body::after {
      content: "";
      position: fixed;
      z-index: -2;
      pointer-events: none;
      border-radius: 999px;
      filter: blur(2px);
    }
    body::before {
      width: 460px; height: 180px; right: -90px; top: 120px;
      background: rgba(255,255,255,.48);
      box-shadow: -360px 120px 0 28px rgba(255,255,255,.32);
      animation: drift 20s ease-in-out infinite alternate;
    }
    body::after {
      width: 220px; height: 220px; left: -80px; bottom: 8%;
      background: rgba(120,213,181,.13);
      box-shadow: 120px 70px 0 36px rgba(105,189,244,.08);
    }
    @keyframes drift { to { transform: translateX(-38px) translateY(10px); } }

    html[data-scene="afternoon"] body {
      background:
        radial-gradient(circle at 12% 10%, rgba(255, 214, 107, .24), transparent 24%),
        radial-gradient(circle at 84% 12%, rgba(120, 213, 181, .18), transparent 30%),
        linear-gradient(180deg, #eafaff 0, #fffaf0 34%, #fffdf8 100%);
    }
    html[data-scene="evening"] body {
      background:
        radial-gradient(circle at 16% 8%, rgba(255, 181, 141, .20), transparent 24%),
        radial-gradient(circle at 88% 8%, rgba(184, 168, 237, .18), transparent 30%),
        linear-gradient(180deg, #f1efff 0, #fff7eb 36%, #fffdf8 100%);
    }

    button, input, textarea, select { font: inherit; }
    button { color: inherit; }
    button:focus-visible, input:focus-visible, textarea:focus-visible, select:focus-visible { outline: 3px solid rgba(105,189,244,.25); outline-offset: 2px; }

    .shell { width: min(1480px, calc(100% - 30px)); margin: 0 auto; padding: 14px 0 60px; }

    .topbar {
      position: sticky; top: 10px; z-index: 50;
      display: grid; grid-template-columns: auto 1fr auto; align-items: center; gap: 18px;
      min-height: 66px; padding: 9px 12px 9px 15px;
      background: rgba(255,253,248,.80); border: 1px solid rgba(255,255,255,.85);
      box-shadow: var(--shadow-soft); backdrop-filter: blur(20px);
      border-radius: 22px;
    }
    .brand { display: flex; align-items: center; gap: 11px; min-width: max-content; }
    .brand-mark {
      width: 42px; height: 42px; display: grid; place-items: center; border-radius: 15px;
      font-weight: 900; color: #174363; background: linear-gradient(145deg, #fff1a9, #8fd8ff);
      box-shadow: inset 0 1px 0 white, 0 9px 25px rgba(93,166,203,.20);
      transform: rotate(-3deg);
    }
    .brand strong { display:block; font-size: 13px; letter-spacing:.08em; }
    .brand span { display:block; color:var(--muted); font-size:11px; }
    .nav { display:flex; justify-content:center; gap:5px; overflow:auto; scrollbar-width:none; }
    .nav::-webkit-scrollbar { display:none; }
    .nav-btn { border:0; background:transparent; padding:9px 13px; color:var(--muted); border-radius:12px; cursor:pointer; white-space:nowrap; transition:.18s ease; }
    .nav-btn:hover { background:#fff; color:var(--ink); }
    .nav-btn.active { color:#174363; background:linear-gradient(135deg, var(--sky-soft), var(--sun-soft)); font-weight:750; }
    .top-actions { display:flex; gap:7px; align-items:center; }

    .btn, .icon-btn {
      border:1px solid var(--line); background:rgba(255,255,255,.76); border-radius:13px;
      box-shadow:0 4px 14px rgba(72,116,139,.06); cursor:pointer; transition:.18s ease;
    }
    .btn { padding:10px 14px; }
    .btn.small { padding:7px 10px; font-size:12px; border-radius:10px; }
    .icon-btn { width:40px; height:40px; display:grid; place-items:center; }
    .btn:hover, .icon-btn:hover { transform:translateY(-1px); border-color:var(--line-strong); box-shadow:var(--shadow-soft); }
    .btn.primary { border-color:transparent; font-weight:800; color:#174363; background:linear-gradient(135deg, #bce9ff, #fff0a3); }
    .btn.mint { border-color:transparent; background:var(--mint-soft); color:#246b57; font-weight:750; }
    .btn.peach { border-color:transparent; background:var(--peach-soft); color:#925334; font-weight:750; }
    .btn.ghost { background:transparent; box-shadow:none; }
    .btn.danger { color:#b84f50; background:var(--coral-soft); }

    main { padding-top:17px; }
    .page { display:none; animation:fade .22s ease; }
    .page.active { display:block; }
    @keyframes fade { from { opacity:0; transform:translateY(5px); } to { opacity:1; transform:none; } }

    .hero {
      position:relative; overflow:hidden; min-height:300px; padding:clamp(25px,4vw,52px);
      display:grid; grid-template-columns:minmax(0,1.25fr) minmax(330px,.75fr); align-items:center; gap:28px;
      border:1px solid rgba(255,255,255,.82); border-radius:var(--r-xl); background:rgba(255,253,248,.72);
      box-shadow:var(--shadow); backdrop-filter:blur(18px);
    }
    .hero::before {
      content:""; position:absolute; width:170px; height:170px; right:7%; top:-55px; border-radius:50%;
      background:radial-gradient(circle at 35% 35%, #fff8c7, var(--sun)); box-shadow:0 0 70px rgba(255,214,107,.34);
      animation:breathe 6s ease-in-out infinite alternate;
    }
    .hero::after {
      content:""; position:absolute; inset:auto 0 0; height:42%; opacity:.45;
      background:
        radial-gradient(ellipse at 15% 100%, rgba(120,213,181,.42) 0 17%, transparent 18%),
        radial-gradient(ellipse at 28% 105%, rgba(105,189,244,.28) 0 20%, transparent 21%),
        radial-gradient(ellipse at 78% 110%, rgba(120,213,181,.30) 0 21%, transparent 22%);
      pointer-events:none;
    }
    @keyframes breathe { to { transform:translateY(8px) scale(1.03); } }
    .hero-copy, .hero-focus { position:relative; z-index:2; }
    .eyebrow { color:#3b92c8; font-size:12px; font-weight:900; letter-spacing:.14em; }
    .hero h1 { margin:13px 0 12px; font-size:clamp(35px,5vw,68px); line-height:1.02; letter-spacing:-.045em; }
    .hero h1 em { font-style:normal; color:#3b92c8; }
    .hero p { max-width:740px; color:var(--muted); font-size:clamp(15px,1.7vw,18px); margin:0; }
    .hero-meta { display:flex; flex-wrap:wrap; gap:8px; margin-top:22px; }
    .chip { display:inline-flex; align-items:center; gap:7px; padding:8px 11px; background:rgba(255,255,255,.65); border:1px solid var(--line); border-radius:999px; font-size:12px; color:var(--muted); }
    .dot { width:8px; height:8px; border-radius:50%; background:var(--mint); box-shadow:0 0 0 4px rgba(120,213,181,.15); }

    .hero-focus { border:1px solid rgba(255,255,255,.82); background:rgba(255,255,255,.62); border-radius:24px; padding:18px; box-shadow:var(--shadow-soft); }
    .hero-focus-head { display:flex; align-items:center; justify-content:space-between; gap:10px; margin-bottom:12px; }
    .hero-focus-head strong { font-size:15px; }
    .focus-mini { display:grid; gap:8px; }
    .focus-mini-item { display:grid; grid-template-columns:26px 1fr auto; align-items:center; gap:9px; padding:9px 10px; border-radius:13px; background:rgba(255,255,255,.72); border:1px solid rgba(47,91,116,.08); }
    .focus-num { width:25px; height:25px; display:grid; place-items:center; border-radius:9px; font-size:11px; font-weight:900; background:var(--sky-soft); color:#297cae; }
    .focus-mini-item span { font-size:12px; min-width:0; white-space:nowrap; overflow:hidden; text-overflow:ellipsis; }
    .focus-mini-item small { color:var(--muted); }

    .section-head { display:flex; justify-content:space-between; align-items:flex-end; gap:16px; margin:32px 3px 13px; }
    .section-head h2 { margin:0; font-size:25px; letter-spacing:-.025em; }
    .section-head p { margin:5px 0 0; color:var(--muted); font-size:13px; }
    .kicker { color:#3b92c8; font-weight:900; letter-spacing:.13em; font-size:11px; }

    .card { background:var(--paper); border:1px solid rgba(255,255,255,.85); box-shadow:var(--shadow-soft); backdrop-filter:blur(14px); border-radius:var(--r-lg); }
    .card.pad { padding:19px; }
    .grid { display:grid; gap:14px; }
    .grid.two { grid-template-columns:repeat(2,minmax(0,1fr)); }
    .grid.three { grid-template-columns:repeat(3,minmax(0,1fr)); }
    .grid.main-side { grid-template-columns:minmax(0,1.45fr) minmax(320px,.55fr); }

    .view-switch { display:flex; gap:5px; padding:4px; border-radius:13px; background:rgba(255,255,255,.62); border:1px solid var(--line); }
    .view-switch button { border:0; padding:7px 11px; border-radius:9px; bac