margin:9px 0 7px; padding:8px 9px; border-radius:11px; color:#4d6679; background:rgba(238,247,251,.75); font-size:11px; }
    .task-why { color:var(--muted); font-size:11px; margin:0 0 8px; }
    .task-meta { display:flex; flex-wrap:wrap; gap:6px; }
    .mini { padding:4px 7px; border-radius:999px; background:#f7fafb; border:1px solid var(--line); color:var(--muted); font-size:10px; }

    .task-table-wrap { overflow:auto; }
    .task-table { width:100%; border-collapse:separate; border-spacing:0; min-width:1040px; }
    .task-table th { text-align:left; padding:10px 9px; font-size:11px; color:var(--muted); border-bottom:1px solid var(--line); white-space:nowrap; }
    .task-table td { padding:10px 9px; border-bottom:1px solid rgba(47,91,116,.07); vertical-align:top; font-size:12px; }
    .task-table tr:hover td { background:rgba(223,243,255,.30); }
    .table-title { font-weight:800; max-width:260px; }
    .table-standard { color:var(--muted); max-width:320px; }
    .checkbox { width:18px; height:18px; accent-color:#4aaee8; }

    .rescue-strip { display:grid; grid-template-columns:auto 1fr auto; gap:12px; align-items:center; padding:14px 16px; background:linear-gradient(100deg,var(--peach-soft),rgba(255,255,255,.78)); border-radius:18px; border:1px solid rgba(255,181,141,.32); }
    .rescue-icon { width:42px; height:42px; display:grid; place-items:center; border-radius:15px; background:white; font-size:20px; }
    .rescue-strip strong { display:block; font-size:14px; }
    .rescue-strip p { margin:2px 0 0; color:var(--muted); font-size:12px; }

    .timeline { display:grid; gap:8px; }
    .time-row { display:grid; grid-template-columns:95px 1fr auto; align-items:center; gap:12px; padding:10px 12px; border-radius:14px; background:rgba(255,255,255,.67); border:1px solid rgba(47,91,116,.07); }
    .time-row.protected { background:linear-gradient(90deg,var(--mint-soft),rgba(255,255,255,.7)); }
    .time-row.meal { background:linear-gradient(90deg,var(--sun-soft),rgba(255,255,255,.7)); }
    .time { font-weight:850; color:#356f90; font-size:12px; }
    .time-row strong { display:block; font-size:13px; }
    .time-row span { color:var(--muted); font-size:11px; }

    .baselines { display:grid; grid-template-columns:repeat(4,minmax(0,1fr)); gap:9px; }
    .baseline { display:flex; gap:9px; align-items:center; padding:12px; border-radius:15px; background:rgba(255,255,255,.72); border:1px solid var(--line); cursor:pointer; }
    .baseline.done { background:var(--mint-soft); border-color:rgba(120,213,181,.35); }
    .baseline input { accent-color:#50b991; }
    .baseline strong { font-size:12px; }
    .baseline span { display:block; font-size:10px; color:var(--muted); }

    .capture-box { display:grid; grid-template-columns:1fr auto auto; gap:8px; }
    .capture-list { display:grid; gap:7px; margin-top:12px; }
    .capture-item { display:grid; grid-template-columns:1fr auto; gap:8px; padding:10px 11px; border-radius:13px; background:rgba(255,255,255,.68); border:1px solid rgba(47,91,116,.07); }
    .capture-item span { font-size:12px; }
    .capture-item small { color:var(--muted); }

    .risk-list { display:grid; gap:8px; }
    .risk-item { display:grid; grid-template-columns:auto minmax(160px,1fr) minmax(140px,1.2fr) auto; gap:11px; align-items:center; padding:11px 12px; background:rgba(255,255,255,.72); border:1px solid rgba(47,91,116,.07); border-radius:14px; }
    .risk-rank { width:28px; height:28px; display:grid; place-items:center; background:var(--coral-soft); color:#a54d4d; border-radius:10px; font-size:11px; font-weight:900; }
    .risk-item strong { font-size:12px; }
    .risk-item span { color:var(--muted); font-size:11px; }
    .risk-level { padding:5px 8px; border-radius:999px; font-size:10px; font-weight:850; }
    .risk-level.high { background:var(--coral-soft); color:#a54d4d; }
    .risk-level.mid { background:var(--sun-soft); color:#806313; }
    .risk-level.low { background:var(--mint-soft); color:#246b57; }

    .project-toolbar { display:flex; gap:8px; flex-wrap:wrap; margin-bottom:12px; }
    .project-table { width:100%; border-collapse:separate; border-spacing:0; min-width:980px; }
    .project-table th { position:sticky; top:0; background:#fffdf8; text-align:left; padding:10px; font-size:11px; color:var(--muted); border-bottom:1px solid var(--line); }
    .project-table td { padding:11px 10px; font-size:12px; border-bottom:1px solid rgba(47,91,116,.07); vertical-align:middle; }
    .progress { width:120px; height:8px; background:#edf4f6; border-radius:999px; overflow:hidden; }
    .progress > i { display:block; height:100%; background:linear-gradient(90deg,var(--mint),var(--sky)); border-radius:inherit; }
    .owner-group { background:linear-gradient(90deg,var(--sky-soft),rgba(255,255,255,.8)); font-weight:850; }

    .review-prompts { display:grid; gap:9px; }
    .prompt { padding:11px 12px; border-radius:14px; background:linear-gradient(110deg,var(--sky-soft),rgba(255,255,255,.72)); font-size:12px; }
    .review-output { white-space:pre-wrap; min-height:210px; padding:15px; border-radius:17px; background:#fffdf8; border:1px solid var(--line); color:#40596b; font-size:12px; }

    .garden { position:relative; min-height:260px; overflow:hidden; background:linear-gradient(180deg,#dff4ff 0,#fff9df 70%,#e8f6df 71%); border-radius:22px; border:1px solid rgba(255,255,255,.9); }
    .garden-sun { position:absolute; width:78px; height:78px; right:13%; top:14%; border-radius:50%; background:radial-gradient(circle at 35% 35%,#fffbd0,var(--sun)); box-shadow:0 0 40px rgba(255,214,107,.38); transition:.5s ease; }
    .garden-cloud { position:absolute; width:115px; height:35px; left:12%; top:20%; border-radius:999px; background:rgba(255,255,255,.78); box-shadow:42px -13px 0 -5px rgba(255,255,255,.78); }
    .plant { position:absolute; bottom:0; width:22px; height:var(--h,60px); left:var(--x); border-radius:20px 20px 3px 3px; background:linear-gradient(180deg,#79d2a8,#4eaa7d); transform-origin:bottom; transition:.6s ease; }
    .plant::before, .plant::after { content:""; position:absolute; width:28px; height:15px; background:#83d9af; border-radius:100% 0 100% 0; top:32%; }
    .plant::before { left:-21px; transform:rotate(12deg); }
    .plant::after { right:-21px; transform:scaleX(-1) rotate(12deg); }
    .garden-copy { position:absolute; left:20px; bottom:18px; z-index:3; }
    .garden-copy strong { display:block; font-size:18px; }
    .garden-copy span { color:#507183; f